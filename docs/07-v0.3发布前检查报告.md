# v0.3 发布前检查报告

> 检查日期：2026-07-17
> 目标版本：v0.3.0
> 分支：`codex/v0.3.0-screen-cast`

## 结论

MatePad 到 Mac 单向投屏的双端实现已经完成，HarmonyOS HAP 构建、macOS 应用构建与 17 项 Swift 测试通过；Mac 可向已配对在线 Pad 发送认证投屏请求，Mac 解码改为 VideoToolbox 实际解码成功后再提交显示。代码具备进入统一真机测试的条件。当前仍不是公开发布候选，因为系统 Picker、硬件编码、持续画面、方向变化、后台行为、长时间稳定性和真实端到端性能只能由目标设备验收。

稳定版 `v0.2.1` 的文件互传与公开下载继续维持；v0.3 分支不得修改已发布 Release 附件，也不能在真机验收前把投屏写成稳定版已发布能力。

## 已完成

- [x] 产品边界：MatePad 到 Mac 单向 H.264 画面，不含声音、控制、副屏或内置录制。
- [x] HarmonyOS 原生屏幕采集、硬件 H.264 编码和 N-API 二进制回调。
- [x] HMCS 独立端口、能力发现、帧上限、有界队列、心跳和有限重连。
- [x] 六位码配对生成 256 位随机共享密钥，媒体与控制载荷使用 AES-256-GCM。
- [x] Mac 配对身份认证、VideoToolbox 解码、独立窗口、画中画、全屏和停止。
- [x] Mac 主动投屏请求、HMAC-SHA256、30 秒时间窗、防重放和 Pad 系统 Picker 调起链路。
- [x] 双端设置、状态、诊断、隐私说明和错误反馈接入。
- [x] 投屏画面不落盘、不进入数据库、不进入文件历史、不上传云端。
- [x] macOS HMCS、主动请求 HMAC 和既有文件传输测试通过，共 17 项。
- [x] HarmonyOS HAP 构建与 macOS 应用构建通过。
- [x] `Index.ets` 低于 1000 行，投屏新增实现按控制器、服务、协议和 Native 模块拆分。
- [x] 开源目录只保留可以随项目公开的产品、技术、协议与验收资料。

## 真机发布阻塞项

- [ ] HarmonyOS 系统录屏授权、拒绝恢复、切换其他应用和系统停止入口真机验证。
- [ ] Mac “发起投屏”在 Pad 前台正确调起系统 Picker；应用退出、旧版和拒绝授权路径提示正确。
- [ ] 硬件编码真实画面、SPS/PPS、关键帧、方向变化和三档画质验证。
- [ ] Mac VideoToolbox 实际解码、普通窗口、画中画、全屏、多显示器和系统录屏验证。
- [ ] Wi-Fi 抖动、断网重连、锁屏、睡眠/唤醒和双方主动停止验证。
- [ ] 深浅色、横竖屏、分屏、浮窗、窄窗口和触摸热区验收。
- [ ] 60 分钟投屏、100 次启停、队列上限、CPU、内存、温度和空闲能耗测量。
- [ ] 投屏与 5 GB / 20 GB+ 文件并行时的正确性和带宽竞争数据。
- [ ] 文件/文件夹、断点续传、多设备、解除配对、后台传输和历史设置全量回归。
- [ ] 覆盖升级、干净安装、旧配对无投屏密钥时重新配对流程。
- [ ] 正式候选 DMG/HAP 的版本、签名、安装、启动和隐私声明复核。

## 当前风险

1. HarmonyOS 屏幕采集授权和后台行为以系统策略为准，构建成功不能证明切换其他应用后仍能持续采集。
2. H.264 编码器输出格式、持续 P 帧、方向变化和 60 fps 请求能力必须用真实画面验证，固定协议测试不能代替解码实测。
3. TCP 首版依靠丢弃旧帧和有界队列控制延迟，弱网和大文件并行仍需要真实网络数据。
4. 旧版配对记录没有投屏共享密钥，投屏会明确要求删除设备后重新配对，这是安全迁移而不是静默降级。
5. macOS 当前没有开发者账号公证能力；测试包可用，但公开分发必须继续如实提示系统安全限制。

## 对外描述规则

- 官网和稳定版 README 继续以 `v0.2.1` 为公开能力基线。
- 开发文档可以写“代码与构建完成、进入真机验收”，不能写“v0.3 已发布”。
- 隐私政策可以提前覆盖开发中的屏幕投放处理边界，但应用市场权限声明必须与最终提交包一致。
- 真机阻塞项全部通过后，再更新本报告、官网、Release Notes 和应用市场文案。
